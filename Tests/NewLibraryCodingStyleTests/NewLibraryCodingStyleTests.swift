import XCTest
@testable import NewLibraryCodingStyle

final class NewLibraryCodingStyleTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NewLibraryCodingStyle().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
